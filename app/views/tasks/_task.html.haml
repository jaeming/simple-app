.all-tasks
  %li.task-list
    = link_to task, id: "task-#{task.id}", class: 'tasks', method: :delete do
      .checkbox-container
        %i.empty.checkbox.large.icon
        %i.checkmark.sign.large.icon
    %p.task-font
      = task.description

